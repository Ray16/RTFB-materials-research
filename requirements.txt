# Pip dependencies for the `redox` env. Installed by setup_env.sh AFTER a CUDA-matched
# torch, so pip sees torch already satisfied and won't swap in a mismatched CUDA build.
# torch itself is pinned here too, so fairchem's dependency resolution keeps the build
# that setup_env.sh installed from the CUDA-specific index.
torch==2.8.0
fairchem-core==2.21.0
ase
rdkit
pyscf
geometric  # geomeTRIC — in-solvent DFT geometry optimizer driven by pyscf
# gpu4pyscf-cuda12x + cutensor-cu12 (GPU DFT+SMD backend) are CUDA-tagged like torch, so
# they are installed by setup_env.sh step 3b (not here) to keep pip's resolver from pulling
# a mismatched CUDA build. On a GPU node the pipeline uses them via `redox.dft --backend gpu`.
pandas
numpy
scipy
matplotlib
graphviz  # python binding for the pipeline flowchart; needs the system `dot` binary
          # (apt: graphviz / conda: graphviz). Rendering falls back gracefully if absent.
jupyter
huggingface_hub
